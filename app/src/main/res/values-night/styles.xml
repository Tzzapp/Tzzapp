<resources>

    <!-- Light application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="backgroundColor">@color/mainBackground</item>
        <item name="mainText">@color/mainText</item>
        <item name="android:background">@color/mainBackground</item>
        <item name="android:titleTextStyle">@color/mainText</item>
        <item name="android:statusBarColor">@color/mainBackground</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="equipmentTheme" parent="Theme.AppCompat.DayNight">
        <!-- Customize your theme here. -->|
        <item name="mainText">@color/mainText</item>
        <item name="android:titleTextStyle">@color/mainText</item>
        <item name="android:statusBarColor">@color/mainBackground</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="actionBarTheme">@style/AppTheme</item>

</style>

    <style name="item" parent="Theme.MaterialComponents.DayNight.Dialog">
        <item name="mainText">@color/mainText</item>
        <item name="backgroundColor">@color/mainBackground</item>


    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="PreferenceTheme" parent="Theme.AppCompat.DayNight">
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
        <item name="android:textColor">@color/mainText</item>
        <item name="android:background">@color/mainBackground</item>
        <item name="android:titleTextColor">@color/mainText</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.DayNight" />

</resources>